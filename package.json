{
  "name": "prefix-commit-jira-id",
  "version": "1.0.0",
  "description": "Adds jira id prefix for each git commit",
  "main": "cli.js",
  "scripts": {
    "commitmsg": "node ./cli.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "bin": {
    "prefix-commit-jira-id": "./cli.js"
  },
  "dependencies": {
    "find-up": "^2.1.0",
    "lodash": "^4.17.10"
  },
  "devDependencies": {
    "husky": "^0.14.3"
  }
}
